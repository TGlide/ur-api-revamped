{
  "name": "netflix-movies-api",
  "version": "1.0.0",
  "description": "api for netflix",
  "dependencies": {
    "cz-emoji": "^1.2.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tglide",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-emoji"
    }
  }
}
